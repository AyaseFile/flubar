import 'package:flutter/material.dart';

const kViewPadding = EdgeInsets.all(4.0);
const kDividerThickness = 4.0;
const kPlaylistViewPadding = EdgeInsets.only(left: 4.0, top: 4.0, bottom: 4.0);
const kTracklistViewPadding = EdgeInsets.only(
  right: 4.0,
  top: 4.0,
  bottom: 4.0,
);
const kTableViewPadding = EdgeInsets.only(
  left: 4.0,
  right: 4.0,
  top: 4.0,
  bottom: 4.0,
);
const kDoubleViewPadding = EdgeInsets.all(8.0);
const kTableTextPadding = EdgeInsets.all(4.0);
const kCellTextPadding = EdgeInsets.only(left: 2.0);
